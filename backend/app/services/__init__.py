# Business Services layer. The single place business logic lives;
# every consumer (routes, AI tools) calls through here, never a
# repository directly (Architecture.md 2.5). Empty until the first
# module's service layer is built.
