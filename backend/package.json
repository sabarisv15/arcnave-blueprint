{
  "name": "arcnave-backend",
  "version": "0.1.0",
  "private": true,
  "description": "ARCNAVE backend — Express (Node.js, plain JavaScript). See docs/adr/ADR-016-Node-Backend.md.",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "migrate": "node scripts/migrate.js up",
    "migrate:down": "node scripts/migrate.js down",
    "test": "node --test tests/"
  },
  "dependencies": {
    "express": "^4.19.2",
    "pg": "^8.12.0"
  },
  "devDependencies": {
    "node-pg-migrate": "^7.6.1"
  }
}
